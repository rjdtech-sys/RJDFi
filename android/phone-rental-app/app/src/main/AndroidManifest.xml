<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Foreground service for timer -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- FOREGROUND_SERVICE_SPECIAL_USE only needed on Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" tools:ignore="ProtectedPermissions" />
    
    <!-- Keep screen on during rental -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Account management for auto-logout -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    
    <!-- Query installed apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    
    <!-- Device admin for kiosk mode -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" tools:ignore="ProtectedPermissions" />

    <!-- OTA update: allow installing APKs downloaded from the server -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Auto-start on boot to maintain kiosk mode after restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Status bar blocker overlay: draw above all apps to block notification shade -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneRental"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Main Launcher Activity -->
        <!-- NOTE: HOME/DEFAULT categories are NOT declared here because they
             prevent installation on regular phones. Instead, the app registers
             as a launcher programmatically when set as Device Owner via
             dpm set-default-launcher. This allows normal sideload installation. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="fullSensor"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize"
            android:stateNotNeeded="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Launcher alias: enabled ONLY when app is set as Device Owner.
             This provides the HOME category for kiosk mode.
             Disabled by default so the app installs normally on any phone. -->
        <activity-alias
            android:name=".ui.MainLauncherAlias"
            android:targetActivity=".ui.MainActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <!-- Setup Activity for configuring server connection -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Admin Activity for app management -->
        <activity
            android:name=".admin.AdminActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Device Admin Receiver for lock task mode -->
        <receiver
            android:name=".admin.KioskDeviceAdmin"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Boot completed receiver - auto-start MainActivity on boot -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Timer Foreground Service -->
        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:targetApi="34" />

        <!-- Status Bar Blocker overlay service (kiosk protection) -->
        <service
            android:name=".service.StatusBarBlockerService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:targetApi="34" />

        <!-- HeartbeatWorker is a WorkManager worker, NOT a service.
             WorkManager auto-registers via AndroidManifest merge.
             Do NOT declare it as <service> or it crashes on startup. -->

        <!-- FileProvider for sharing downloaded APK with system installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

    <!-- Required for querying installed apps on Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

</manifest>
